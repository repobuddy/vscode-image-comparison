# Image Comparison

Compare two images in a VS Code webview using a draggable slider.

## Usage

1. Run the command `Image Comparison: Compare Two Images`.
2. Pick exactly two image files.
3. Drag the slider to reveal either image.

Supported formats include png, jpg, jpeg, gif, webp, bmp, and tiff.
